name: build_and_publish

on:
  workflow_dispatch: { }
  push:
    branches:
      - main

jobs:
  build_and_publish:
    uses: PhiTiet/workflows/.github/workflows/build_and_publish.yml@main
    with:
      tag: portfolio